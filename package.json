{
  "name": "chronolux-fullstack",
  "version": "1.0.0",
  "description": "Luxury watches e-commerce fullstack application",
  "scripts": {
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:admin": "npm run dev --prefix admin-panel",
    "dev:gateway": "node gateway.js",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:admin\" \"npm run dev:gateway\"",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend && npm install --prefix admin-panel"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "http-proxy": "^1.18.1"
  }
}
